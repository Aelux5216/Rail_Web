@{
    ViewData["Title"] = "Results";
}

<h2 class="text-left">Departure Times</h2>
<p id="departureBox" class="text-left">
    @{

        List<string> resultList = resultModel.resultValue;

        foreach (string s in resultList)
        {
            @s;
        }
    }
</p>
